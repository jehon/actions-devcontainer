name: Makefile

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
  workflow_dispatch:

jobs:
  ga-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - uses: jehon/actions-devcontainer@main
      with:
        action: dump

    - uses: jehon/actions-devcontainer@main
      with:
        action: start

    - uses: jehon/actions-devcontainer@main
      with:
        action: make
        make: dump
